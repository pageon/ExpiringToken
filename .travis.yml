language: php

matrix:
  include:
    - php: 7.0
    - php: 7.1
    - php: hhvm
  allow_failures:
    - php: hhvm

script:
  - phpunit
  - vendor/bin/phpcs --standard=psr2 --extensions=php --warning-severity=0 --report=full "src"

before_script:
  - composer install

sudo: false
